/**
 * コンポーネント「レイアウト > ページ」の定義。
 *
 * ページは URL と1対1に対応する概念であり、共有されるコンポーネントを持たない。
 * ここで定義するのは、すべてのページが従う規則である。
 */

export type PageFile = {
  file: string;
  role: string;
  constraint: string;
};

/** ページの実装は2つのファイルに分かれる(規約 web.md §1)。 */
export const PAGE_FILES: PageFile[] = [
  {
    file: "src/app/<route>/page.tsx",
    role: "ルーティング。画面本体を組み立てるだけの薄いラッパー。",
    constraint:
      'Server Component のまま保つ。"use client" を書かない。ロジックとマークアップを持たない。',
  },
  {
    file: "src/app/tabs/<画面>.tsx",
    role: "画面本体。フレームを置き、製品を並べる。",
    constraint:
      '"use client"。肥大化したら tabs/<画面>/ に画面専用の子コンポーネントを分割する。',
  },
];

export type PageRule = {
  title: string;
  body: string;
};

export const PAGE_RULES: PageRule[] = [
  {
    title: "必ずフレームを使う",
    body: "ページは自分で高さやスクロールを組まない。フレームを1つ置き、その中に製品を並べる。フレームを入れ子にしない。",
  },
  {
    title: "共有するのはコンポーネントではなく規則",
    body: "ページは URL と1対1であり、他のページと共有される部分を持たない。共通する見た目はフレーム、共通する機能は製品として切り出す。ページ自身に共通コンポーネントを作らない。",
  },
  {
    title: "保持したい UI 状態は URL に置く",
    body: "リロードやリンク共有で再現したい状態(選択中のタブ、選択中の項目)は URL クエリで表す(?tab= / ?item=)。モーダルの開閉や入力途中のような一時的な状態は、コンポーネントローカルに持ってよい。",
  },
  {
    title: "データを取得しない",
    body: "取得口を持ってよいのは製品だけである。ページは製品を配置するところまでを担い、データの取得と保持には関与しない。",
  },
  {
    title: "幅は用途で選ぶ",
    body: "読み物は width.prose(720px)、表や図を含む文書は width.doc(1080px)、図表そのものは width.full。同じページの中でブロックごとに変えてよい。",
  },
  {
    title: "縦スクロールはページ全体で持たない",
    body: "スクロールはフレームが分割した領域の内側で持つ。ページ全体が縦に伸びると、ナビゲーションが画面外へ流れて現在地を見失う。",
  },
];
